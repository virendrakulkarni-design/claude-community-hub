name: Daily Claude Architectural Publisher

on:
  schedule:
    # Daily at 03:00 UTC = 8:30 AM IST
    - cron: '0 3 * * *'
  workflow_dispatch:
    inputs:
      custom_topic:
        description: 'Suggest custom topic/keyword (e.g. Graph Engineering, Certifications, Multi-Agent)'
        required: false
        default: ''

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  publish_and_deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install anthropic

      - name: Execute Automated Post Generator
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CUSTOM_TOPIC: ${{ github.event.inputs.custom_topic }}
        run: |
          if [ -n "$CUSTOM_TOPIC" ]; then
            echo "🚀 Generating custom suggested topic: $CUSTOM_TOPIC"
            python scripts/generate_post.py --custom-topic "$CUSTOM_TOPIC"
          else
            echo "📅 Generating next scheduled topic from queue"
            python scripts/generate_post.py
          fi

      - name: Commit and Push Updated Knowledge Base
        run: |
          git config --global user.name "Claude Architecture Bot"
          git config --global user.email "bot@users.noreply.github.com"
          git add data/
          git diff --quiet && git diff --staged --quiet || git commit -m "Auto-publish: New daily architectural dispatch [skip ci]"
          git push

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
